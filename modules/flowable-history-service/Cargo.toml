[package]
name = "flowable-history-service"
version = "0.1.0"
edition = "2024"

[dependencies]
flowable-engine = { path = "../flowable-engine" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
